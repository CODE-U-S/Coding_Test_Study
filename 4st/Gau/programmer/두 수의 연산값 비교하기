class Solution {
    public int solution(int a, int b) {
        int ab = Integer.parseInt(String.valueOf(a) + String.valueOf(b));
        
        int mul = 2 * a * b;
        
        if (ab >= mul) {
            return ab;
        } else {
            return mul;
        }
    }
}